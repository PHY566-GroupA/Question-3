if z == 5000: 
        for i in range (0,x):
            count_1=0
            count_2=0
            for j in range (0,y):
                if a[i,j] == 1:
                    count_1=count_1+1
                if a[i,j] == 2:
                    count_2=count_2+1
            density_1.append(count_1/float(y))
            density_2.append(count_2/float(y))
        print a
